Okay so bash is mostly linux unix terminal shell that use some cool commands and use to create some cool scripts for automations and many more so from far what i know about terminal is that it is a cli interface where you can jsut type and run bash programs easily

there is going to be some things i am not going to type in here cause i can get used to it soon... hopefull ;)


1. cd, pwd, cat ,touch, ls, mkdir - these are some basic commands that use most of the time cd to change directories pwd to view what directory we are in cat shows the output of the file's data ls shows all file of the directory you are in and mkdir create directories 
2. echo - this is the command that prints our own text like 
   `echo "You are a Homo Sepian"` this will print the exact thing that you want to echo 
3. grep - this is like a finder there is multiple functions and above commands too have mulltiple things that we discuss later 
4. pipelines - this thing is a bitxh it can run multiple command for a single file at same time this is the guy `|`. for example
   
   `echo atif > file.txt`
   `echo ATIF >> file.txt`
   `cat file.txt | grep -io "^t."`
   
   this prints what we wanted also the thing `^t.` i used and that `-io` in grep is more functions of grep command that we are gonna discuss later

Here are the some links that are usefull 

[[Pagers]] 
[[Man Pages (manuals of tui)]] 
[[Loops]]
[[If Else statements]]
[[Functions]]
[[Conditionals]]
[[Case Statements]]
[[Arithmetic Expressions in Bash]]
[[Arrays]]
[[Process Substitution]]
[[Usefull Commands (Must!)]]
[[IFS Vars]]