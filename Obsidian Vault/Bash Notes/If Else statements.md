the if else statements is pretty fun it is like run a command only if meet the required condition else run other one the syntax is

`if COMMAND; then`
	`echo "Hello COMMAND"`
`else`
	`echo "error state"`
`fi`

to know more about commands function like -n -z and more shits see MAN page of TEST

i am not gonna right more abotu it you can get a look into it and see here is the example script 

![[Pasted image 20260702164843.png]]


Some links

[[Loops]]
