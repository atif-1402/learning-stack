
`#include <stdio.h>

`int main() {

`     return 0;
`} `

This is the basic program of C which give return code 0 means it works fine without errors we can use a basic command like `printf("")` to print the lines its like echo from bash print from python 

`#include <stdio.h>`

`int main() {`

	printf("Hi my name is Atif");
	return 0;
`}

every line inside the entry point should end with semi-colon (entry point is `int main() {}` )   

[[2) Varibles in C]]